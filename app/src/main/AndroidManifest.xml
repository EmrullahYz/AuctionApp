<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.examples.firebaseauth">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".EkleMenu" />
        <activity android:name=".MyListMenu" />
        <activity android:name=".HesapMenu" />
        <activity android:name=".AntikaDetayActivity" />
        <activity android:name=".EkspertizMainActivity"/>
        <activity android:name=".EkspertizLoginActivity"/>
        <activity android:name=".AntikaDetayForEkspertizActivity"/>
        <activity android:name=".EkspertizHesapActivity"/>


    </application>

</manifest>